version: '3.8'

services:
  web:
    build: .
    command: python /dockerprojects/manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/dockerprojects
    ports:
      - 8000:8000
    depends_on:
      - db
  db:
    image: postgres:12
    environment:
      - POSTGRES_PASSWORD=love2life4ever